(()=>{const q=s=>document.querySelector(s);async function checkout(plan){const u=await window.SFAuth?.current?.();if(!u){location.href='login.html?redirect=earn.html';return}const provider=q('#paymentProvider')?.value||'stripe',p=window.ULMFLIX_CONFIG?.PLANS?.[plan];if(!p)return;let endpoint='/api/create-checkout-session';if(provider==='sslcommerz')endpoint='/api/create-sslcommerz-session';if(provider==='bkash')endpoint='/api/bkash-create';if(provider==='nagad')return alert('Nagad is not enabled until a valid merchant API contract and RSA credentials are configured.');try{const session=await window.SF_SUPABASE.auth.getSession();const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.data.session.access_token}`},body:JSON.stringify({planId:p.id})});const d=await r.json();if(d.url){location.href=d.url;return}alert(d.error||'Payment gateway is not configured yet.')}catch(e){alert(e.message||'Payment gateway is not configured yet.')}}document.addEventListener('DOMContentLoaded',()=>{q('#monthlyPlan')?.addEventListener('click',()=>checkout('monthly'));q('#yearlyPlan')?.addEventListener('click',()=>checkout('yearly'));})})();
